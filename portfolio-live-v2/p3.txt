x; font-size:24px; letter-spacing:-.03em; }
.fact-list ul { list-style:none; padding:0; margin:0; display:grid; gap:12px; }
.fact-list li { padding:14px 15px; border-radius:16px; background:rgba(255,255,255,.62); border:1px solid rgba(12,35,52,.05); color:var(--muted); font-size:14px; line-height:1.58; }
.fact-list li strong { display:block; color:var(--text); margin-bottom:4px; font-size:14px; }

.project-list { display:grid; gap:18px; margin-top:28px; }
.project-card { display:grid; grid-template-columns:72px .9fr 1.1fr; gap:20px; align-items:center; padding:24px; border-radius:28px; }
.project-number { color:var(--blue); font-size:12px; font-weight:800; letter-spacing:.12em; }
.project-screen { min-height:240px; border-radius:20px; overflow:hidden; border:1px solid rgba(12,35,52,.08); background:linear-gradient(145deg, rgba(250,252,253,.92), rgba(236,244,248,.92)); box-shadow:inset 0 1px 0 rgba(255,255,255,.85); }
.project-screen-bar { height:38px; border-bottom:1px solid rgba(12,35,52,.08); display:flex; align-items:center; gap:6px; padding:0 14px; }
.project-screen-bar span { width:8px; height:8px; border-radius:50%; background:rgba(15,95,134,.24); }
.project-screen-body { height:calc(100% - 38px); display:flex; flex-direction:column; justify-content:center; padding:24px; }
.project-screen-body small { color:var(--blue); font-size:12px; font-weight:800; letter-spacing:.1em; text-transform:uppercase; }
.project-screen-body strong { margin-top:10px; font-size:32px; letter-spacing:-.035em; }
.project-lines { display:grid; gap:10px; margin-top:20px; }
.project-lines i { display:block; height:12px; border-radius:999px; background:linear-gradient(90deg, rgba(15,95,134,.18), rgba(15,95,134,.04)); }
.project-lines i:nth-child(1) { width:84%; }
.project-lines i:nth-child(2) { width:58%; }
.project-lines i:nth-child(3) { width:92%; }
.project-note { margin-top:12px; display:inline-block; font-size:12px; color:var(--muted); }
.project-eyebrow { margin:0; color:var(--blue); font-size:12px; font-weight:800; letter-spacing:.11em; text-transform:uppercase; }
.project-content h3 { margin:10px 0 12px; font-size:34px; letter-spacing:-.04em; }
.project-content p:not(.project-eyebrow) { margin:0; color:var(--muted); line-height:1.76; }
.tag-list { display:flex; flex-wrap:wrap; gap:8px; margin-top:18px; }
.tag-list span { padding:9px 11px; border-radius:999px; border:1px solid rgba(12,35,52,.08); background:rgba(255,255,255,.74); font-size:12px; color:#445565; }

.dark-band { background:linear-gradient(180deg, rgba(12,34,50,.96), rgba(10,30,45,.93)); color:#f7fbfd; position:relative; z-index:1; }
.dark-band .section { padding-top:82px; padding-bottom:82px; }
.dark-band .kicker, .dark-band .kicker::before { color:#a6dbef; background:#a6dbef; }
.dark-band .section-lead { color:rgba(236,245,248,.68); }
.skills-grid { display:grid; grid-template-columns:repeat(3,1fr); gap:16px; margin-top:28px; }
.skill-card { padding:24px; border-radius:24px; background:rgba(255,255,255,.08); border:1px solid rgba(255,255,255,.12); backdrop-filter:blur(16px); box-shadow:0 22px 44px rgba(0,0,0,.12); }
.skill-card h3 { margin:8px 0 14px; font-size:24px; letter-spacing:-.03em; }
.skill-card .tag-list span { background:rgba(255,255,255,.10); border-color:rgba(255,255,255,.12); color:#ecf8fd; }

.edu-list { display:grid; gap:16px; margin-top:28px; }
.edu-card { display:grid; grid-template-columns:60px 1fr auto; gap:18px; align-items:start; padding:24px; border-radius:24px; }
.edu-index { width:52px; height:52px; border-radius:16px; display:grid; place-items:center; background:linear-gradient(135deg, rgba(15,95,134,.10), rgba(15,95,134,.18)); color:var(--blue); font-weight:800; }
.edu-card small { display:block; color:var(--blue); font-size:12px; font-weight:800; letter-spacing:.11em; text-transform:uppercase; margin-bottom:8px; }
.edu-card h3 { margin:0 0 10px; font-size:28px; letter-spacing:-.035em; }
.edu-card p { margin:0; color:var(--muted); line-height:1.7; max-width:760px; }
.edu-card strong { white-space:nowrap; font-size:16px; }

.contact-grid { display:grid; grid-template-columns:1fr .74fr .74fr; gap:18px; margin-top:20px; align-items:stretch; }
.contact-panel { padding:28px; border-radius:28px; }
.contact-panel.dark { background:linear-gradient(180deg, rgba(13,47,69,.96), rgba(10,36,54,.96)); color:#fff; }
.contact-panel.dark p { color:rgba(255,255,255,.74); line-height:1.7; }
.contact-links { display:flex; flex-direction:column; justify-content:center; gap:10px; }
.contact-links a { display:flex; align-items:center; justify-content:space-between; gap:12px; padding:16px 18px; border-radius:18px; background:rgba(255,255,255,.08); border:1px solid rgba(255,255,255,.10); color:#fff; font-weight:700; }
.contact-links a:hover { background:rgba(255,255,255,.12); }
.contact-photo { position:relative; overflow:hidden; border-radius:28px; min-height:340px; border:1px solid rgba(255,255,255,.8); box-shadow:0 28px 60px rgba(11,37,54,.14); }
.contact-photo img { position:absolute; inset:0; width:100%; height:100%; object-fit:cover; }
.contact-photo::after { content:""; position:absolute; inset:0; background:linear-gradient(180deg, rgba(8,16,28,0) 35%, rgba(8,16,28,.14) 100%); }
footer { width:min(100%, var(--max)); margin:0 auto; padding:0 22px 36px; display:flex; justify-content:space-between; gap:10px; color:var(--muted); font-size:13px; position:relative; z-index:1; }

.reveal { opacity:0; transform:translateY(18px); transition:opacity .58s ease, transform .58s ease; }
.reveal.visible { opacity:1; transform:none; }

@keyframes spin { from { transform:rotateZ(0deg); } to { transform:rotateZ(360deg); } }
@keyframes breathe {
  0%,100% { transform:scale(1) translate3d(0,0,0); opacity:.88; }
  50% { transform:scale(1.08) translate3d(0,-8px,0); opacity:1; }
}
@keyframes orbPulse {
  0%,100% { transform:translate3d(0,0,0) scale(1); }
  50% { transform:translate3d(0,-10px,0) scale(1.05); }
}
@keyframes bubbleFloat1 {
  0%,100% { transform:translate3d(0,0,72px) scale(1); }
  25% { transform:translate3d(14px,-22px,96px) scale(1.03); }
  50% { transform:translate3d(-10px,-36px,82px) scale(1.06); }
  75% { transform:translate3d(-18px,-12px,110px) scale(.98); }
}
@keyframes bubbleFloat2 {
  0%,100% { transform:translate3d(0,0,110px) scale(1); }
  25% { transform:translate3d(-12px,-16px,132px) scale(1.04); }
  50% { transform:translate3d(18px,-28px,118px) scale(1.08); }
  75% { transform:translate3d(8px,6px,104px) scale(1); }
}
@keyframes bubbleFloat3 {
  0%,100% { transform:translate3d(0,0,122px) scale(1); }
  30% { transform:translate3d(8px,-18px,142px) scale(1.05); }
  60% { transform:translate3d(-14px,-26px,128px) scale(.97); }
}
@keyframes bubbleFloat4 {
  0%,100% { transform:translate3d(0,0,90px) scale(1); }
  35% { transform:translate3d(-20px,-26px,118px) scale(1.05); }
  70% { transform:translate3d(16px,-12px,98px) scale(.99); }
}
@keyframes particleDrift {
  0%,100% { transform:translate3d(0,0,0); opacity:.7; }