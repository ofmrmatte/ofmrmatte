ite reverse; }
.bubble.b5::before { width:24px; height:24px; left:14px; top:10px; }
.bubble.b6 { width:58px; height:58px; right:536px; top:388px; transform:translateZ(96px); animation:bubbleFloat3 7.6s ease-in-out infinite reverse; }
.bubble.b6::before { width:18px; height:18px; left:12px; top:10px; }
.scene-particle {
  position:absolute; width:18px; height:18px; border-radius:50%;
  background:radial-gradient(circle at 30% 30%, #fff, rgba(255,255,255,.15));
  box-shadow:0 0 0 1px rgba(255,255,255,.55), 0 0 30px rgba(127,196,222,.52);
  animation:particleDrift 10s ease-in-out infinite;
}
.scene-particle.p1 { right:190px; top:168px; }
.scene-particle.p2 { right:512px; top:316px; width:12px; height:12px; animation-delay:-2s; }
.scene-particle.p3 { right:250px; top:520px; width:14px; height:14px; animation-delay:-5s; }

.app { position:relative; z-index:1; }
.site-header {
  position:sticky; top:14px; z-index:30;
  width:min(calc(100% - 22px), var(--max)); margin:14px auto 0;
  display:flex; align-items:center; justify-content:space-between; gap:16px;
  padding:12px 16px 12px 18px; border-radius:18px;
  border:1px solid rgba(255,255,255,.76); background:rgba(255,255,255,.68); backdrop-filter:blur(18px);
  box-shadow:0 16px 36px rgba(11,36,53,.07);
}
.brand { display:flex; align-items:center; gap:12px; min-width:0; }
.brand-mark {
  width:40px; height:40px; border-radius:12px; display:grid; place-items:center; font-weight:800; color:#fff;
  background:linear-gradient(135deg, #103b58, #16739c);
}
.brand-copy strong { display:block; font-size:14px; }
.brand-copy small { display:block; font-size:12px; color:var(--muted); white-space:nowrap; }
.nav { display:flex; align-items:center; gap:24px; color:var(--muted); font-size:14px; }
.nav a:hover { color:var(--text); }
.menu-button { display:none; background:none; border:0; padding:8px; cursor:pointer; }
.menu-button span { display:block; width:22px; height:2px; background:var(--text); margin:4px 0; border-radius:4px; }

.section, .hero { width:min(100%, var(--max)); margin:auto; padding:78px 22px; }
.hero { display:grid; grid-template-columns:1.05fr .95fr; gap:54px; align-items:center; padding-top:78px; }
.status-line { font-size:12px; letter-spacing:.12em; text-transform:uppercase; color:var(--blue); font-weight:800; }
.hero-role { margin:18px 0 10px; color:var(--muted); font-size:14px; font-weight:600; }
h1 { font-size:clamp(48px, 6vw, 84px); line-height:.94; letter-spacing:-.055em; margin:0 0 20px; max-width:760px; }
.hero-summary { color:var(--muted); line-height:1.75; font-size:18px; max-width:650px; margin:0; }
.hero-actions { display:flex; flex-wrap:wrap; gap:12px; margin:32px 0; }
.button { display:inline-flex; align-items:center; gap:8px; padding:14px 18px; border-radius:999px; font-weight:800; transition:transform .16s ease; }
.button:hover { transform:translateY(-1px); }
.button.primary { background:linear-gradient(135deg, #0f5f86, #0a749e); color:#fff; box-shadow:0 18px 32px rgba(15,95,134,.20); }
.button.secondary { background:rgba(255,255,255,.74); border:1px solid rgba(12,35,52,.08); }
.hero-meta { display:grid; grid-template-columns:repeat(3,1fr); gap:12px; max-width:640px; }
.hero-meta div, .glass-card, .project-card, .edu-card, .contact-panel {
  background:var(--surface); border:1px solid rgba(255,255,255,.78); backdrop-filter:blur(18px); box-shadow:0 22px 55px rgba(11,37,54,.08);
}
.hero-meta div { padding:18px 16px; border-radius:18px; }
.hero-meta span { display:block; color:var(--blue); font-size:11px; font-weight:800; letter-spacing:.12em; }
.hero-meta strong { display:block; margin-top:4px; font-size:16px; }
.hero-meta small { color:var(--muted); font-size:12px; }

.hero-visual { display:grid; grid-template-columns:1fr .72fr; gap:18px; align-items:end; }
.photo-card { position:relative; min-height:600px; overflow:hidden; border-radius:30px; border:1px solid rgba(255,255,255,.8); box-shadow:0 30px 80px rgba(12,37,54,.18); }
.photo-card img { position:absolute; inset:0; width:100%; height:100%; object-fit:cover; }
.photo-card::after { content:""; position:absolute; inset:0; background:linear-gradient(180deg, rgba(8,16,28,0) 38%, rgba(8,16,28,.32) 100%); }
.photo-caption { position:absolute; left:18px; right:18px; bottom:18px; z-index:1; padding:14px 15px; border-radius:18px; background:rgba(8,26,40,.32); backdrop-filter:blur(12px); color:#fff; }
.photo-caption strong { display:block; font-size:16px; }
.photo-caption small { display:block; margin-top:4px; font-size:12px; opacity:.88; }
.side-stack { display:grid; gap:18px; }
.glass-card { padding:22px; border-radius:24px; }
.glass-card small { display:inline-block; color:var(--blue); font-weight:800; font-size:11px; letter-spacing:.11em; margin-bottom:8px; }
.glass-card h3 { margin:0 0 8px; font-size:24px; letter-spacing:-.03em; }
.glass-card p { margin:0; color:var(--muted); line-height:1.65; font-size:14px; }
.stat-line { margin-top:16px; display:grid; gap:10px; }
.stat-line div { display:flex; align-items:center; justify-content:space-between; gap:10px; font-size:13px; color:var(--muted); }
.stat-line b { color:var(--text); }
.highlight-box { padding:20px 22px; border-radius:24px; background:linear-gradient(135deg, #0e425f, #0d678e); color:#fff; box-shadow:0 24px 48px rgba(13,82,117,.24); }
.highlight-box small { display:block; opacity:.72; font-size:11px; letter-spacing:.12em; text-transform:uppercase; }
.highlight-box strong { display:block; margin-top:10px; font-size:24px; letter-spacing:-.03em; }
.highlight-box span { display:block; margin-top:6px; color:rgba(255,255,255,.82); line-height:1.55; font-size:13px; }

.kicker { display:inline-flex; align-items:center; gap:8px; color:var(--blue); font-size:12px; font-weight:800; letter-spacing:.12em; text-transform:uppercase; }
.kicker::before { content:""; width:26px; height:1px; background:var(--blue); }
h2 { font-size:clamp(36px,4vw,58px); line-height:1.02; letter-spacing:-.045em; margin:14px 0 14px; }
.section-lead { color:var(--muted); font-size:16px; line-height:1.72; max-width:760px; margin:0; }
.about-layout { display:grid; grid-template-columns:.9fr 1.1fr; gap:22px; margin-top:28px; }
.about-card { padding:24px; border-radius:26px; }
.about-card.lead { font-size:22px; line-height:1.55; }
.about-card p { color:var(--muted); font-size:16px; line-height:1.78; margin:0 0 16px; }
.about-card p:last-child { margin-bottom:0; }
.about-extras { display:grid; grid-template-columns:1.05fr .95fr; gap:22px; margin-top:22px; }
.about-photo { position:relative; min-height:420px; overflow:hidden; border-radius:26px; border:1px solid rgba(255,255,255,.8); box-shadow:0 24px 56px rgba(11,37,54,.10); }
.about-photo img { position:absolute; inset:0; width:100%; height:100%; object-fit:cover; }
.about-photo::after { content:""; position:absolute; inset:0; background:linear-gradient(180deg, rgba(8,16,28,0) 35%, rgba(8,16,28,.18) 100%); }
.fact-list { padding:24px; border-radius:26px; }
.fact-list h3 { margin:6px 0 14p