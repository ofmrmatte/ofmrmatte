name: portfolio polish

on:
  push:
    branches:
      - portfolio-site
    paths:
      - .github/workflows/portfolio-polish.yml

permissions:
  contents: write

jobs:
  polish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: portfolio-site
          fetch-depth: 0

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Apply portfolio polish
        run: python3 scripts/portfolio_polish.py

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Build
        run: npm run build

      - name: Commit validated changes
        run: |
          rm -f .github/workflows/portfolio-polish.yml scripts/portfolio_polish.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Refine education section and compact contact footer"
          git push origin HEAD:portfolio-site
